substitutions:
  _SERVICE_NAME: menthe-api
  _REGION: asia-southeast1
  _API_IMAGE: gcr.io/$PROJECT_ID/menthe-api:$COMMIT_SHA

steps:
  - name: gcr.io/cloud-builders/docker
    id: Build API image
    env:
      - DOCKER_BUILDKIT=1
    args:
      - build
      - -f
      - Dockerfile.api
      - -t
      - ${_API_IMAGE}
      - .

  - name: gcr.io/cloud-builders/docker
    id: Push API image
    args:
      - push
      - ${_API_IMAGE}

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    id: Deploy API to Cloud Run
    entrypoint: gcloud
    args:
      - run
      - deploy
      - $_SERVICE_NAME
      - --image
      - ${_API_IMAGE}
      - --region
      - $_REGION
      - --platform
      - managed
      - --allow-unauthenticated
      - --update-secrets
      - GEMINI_API_KEY=menthe-gemini-api-key:latest

images:
  - ${_API_IMAGE}

options:
  logging: CLOUD_LOGGING_ONLY

